class Solution:
    def isMatch(self, s: str, p: str) -> bool:
        # dp[i][j] means s[:i] matches p[:j]
        dp = [[False] * (len(p) + 1) for _ in range(len(s) + 1)]
        dp[0][0] = True
        
        # Handle patterns like "*", "**", etc. matching an empty string
        for j in range(1, len(p) + 1):
